# Submission status

Prepared: code, reproducible model training, evaluation, shared Bob MCP engine, local API/UI, public WSGI adapter, Vercel/Render configuration, deployment documentation, screenshots and the latest eight-slide presentation/slides.pptx.

Public repository created from the official template: https://github.com/aryan13102006/bob-ai-hackathon-gridwatch

The organizer workflow and CONTRIBUTING.md match the official template exactly. A separate Backend tests workflow checks model training and serving. Public deployment is in progress; a configuration file is not proof of a live service.

Still required before submission:

1. Confirm the provisional team name GridWatch and add any additional members. Lead details use the requested Aryan Chanpa account.
2. Verify the configured MCP server in a signed-in IBM Bob session and record its actual tool usage.
3. Record the 3–5 minute demonstration using demo/demo-script.md, upload it and replace demo/demo-video-link.txt with an accessible URL.
4. Verify Vercel and Render deployments, update the live-demo URL, and check GitHub Actions on the final commit.
5. Submit the repository URL through the organiser's entry form. Its URL has not been supplied.

Do not treat the pending video file or unverified Bob UI connection as complete. A public repository and live website do not constitute entry-form submission.
